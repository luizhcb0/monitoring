<%= form_for @user, url: {action: :password_update}, html: {class: 'form-horizontal'} do |f| %>
<div class="row">
  <div class="col-sm-8 col-sm-offset-2">

    <div class="">
      <h2>Alteração de Senha</h2>
    </div>

    <div class="form-group">
      <%= f.label :new_password, "Nova Senha", class: "col-sm-3 control-label"%>
      <% if @minimum_password_length %>
        <em>(<%= @minimum_password_length %> characters minimum)</em>
      <% end %>
      <div class="col-sm-8">
        <%= f.password_field :password, class: "form-control", required: "required", autocomplete: "off" %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :password_confirmation, "Digite Novamente", class: "col-sm-3 control-label"%>
      <div class="col-sm-8">
        <%= f.password_field :password_confirmation, class: "form-control", required: "required", autocomplete: "off" %>
      </div>
    </div>

    <%= f.submit "Salvar", class: "btn btn-theme" %>

  </div>
</div>
<% end %>
